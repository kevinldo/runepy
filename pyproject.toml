[build-system]
requires = ["setuptools>=82"]
build-backend = "setuptools.build_meta"

[project]
name = "runepy"
version = "0.1.0"
description = "RuneScape hiscore API and data service"
requires-python = ">=3.14"

[tool.setuptools.packages.find]
where = ["src"]
